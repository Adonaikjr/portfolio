import styled from 'styled-components'

export const ContainerProject = styled.main`
  display: flex;
  padding-top: 1rem;
  justify-content: center;
  flex-wrap: wrap;
  align-items: center;
  gap: 1rem;
`
